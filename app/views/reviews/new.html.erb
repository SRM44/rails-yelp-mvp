<h1>Add a review for this restaurant</h1>
<div>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 0..5 %>
    <%= f.submit "add", class: "btn btn-primary" %>
  <% end %>
</div>
